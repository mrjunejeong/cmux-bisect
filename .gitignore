# Secrets — never commit
.env
.env.*
*.env
.envrc
secrets.json
*.key
*.pem

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
dist/
.vercel

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# cmux-bisect runtime (NOT viewer/public/demo-status.json — that one we DO commit
# as the frozen demo Vercel serves; CLI overwrites it locally for live dev,
# but only the latest committed version reaches production)
cmux-bisect.db
cmux-bisect.db-journal
cmux-bisect.json
.cmux-bisect.json.tmp
status.json
*.session.jsonl
tmp-demo/
